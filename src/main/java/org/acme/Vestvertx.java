package org.acme;

import jakarta.inject.Singleton;

@Singleton
public class Vestvertx {

    private Vestvertx() {

    // start up consumers

    // start up publishers

    // set up sequencer

    // 1) populate processing list from db (unprocessed)

    // 2) Send unprocessed to processing channel when they're ready to be processed

    }

}
